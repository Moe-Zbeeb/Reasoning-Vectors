#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

MODELS=${MODELS:-"XiaomiMiMo/MiMo-7B-SFT XiaomiMiMo/MiMo-7B-RL XiaomiMiMo/MiMo-7B-Base"}
GPUS=${GPUS:-"0,1,2"}
OUTPUT_ROOT=${OUTPUT_ROOT:-"outputs/mimo_official_vllm"}
BENCHMARKS=${BENCHMARKS:-"aime25x8,amc23x8,aime24x8,minerva_math,olympiadbench,math500"}
MAX_TOKENS_PER_CALL=${MAX_TOKENS_PER_CALL:-16384}
N_SAMPLING=${N_SAMPLING:-1}
NUM_TEST_SAMPLE=${NUM_TEST_SAMPLE:--1}
VLLM_BATCH_SIZE=${VLLM_BATCH_SIZE:-8}
VLLM_MAX_MODEL_LEN=${VLLM_MAX_MODEL_LEN:-32768}
VLLM_GPU_MEMORY_UTILIZATION=${VLLM_GPU_MEMORY_UTILIZATION:-0.9}
OVERWRITE=${OVERWRITE:-1}

MODELS="$MODELS" \
GPUS="$GPUS" \
OUTPUT_ROOT="$OUTPUT_ROOT" \
AIME_BENCHMARKS="$BENCHMARKS" \
MATH_BENCHMARKS="" \
MAX_TOKENS_PER_CALL="$MAX_TOKENS_PER_CALL" \
N_SAMPLING="$N_SAMPLING" \
NUM_TEST_SAMPLE="$NUM_TEST_SAMPLE" \
VLLM_BATCH_SIZE="$VLLM_BATCH_SIZE" \
VLLM_MAX_MODEL_LEN="$VLLM_MAX_MODEL_LEN" \
VLLM_GPU_MEMORY_UTILIZATION="$VLLM_GPU_MEMORY_UTILIZATION" \
OVERWRITE="$OVERWRITE" \
bash sh/eval_models_multi_gpu_vllm.sh
