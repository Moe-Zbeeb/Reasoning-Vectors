# Core training stack
torch==2.9.0
transformers==4.57.6
datasets==4.8.4
accelerate==1.13.0
trl==0.29.1
peft==0.18.1
bitsandbytes==0.49.2

# Inference — pinned to 0.12.0 to stay within TRL's supported range
# (vllm ≥0.13 bundles libicui18n.so.78 which requires CXXABI_1.3.15,
#  unavailable on Ubuntu 20.04's system libstdc++)
vllm==0.12.0

# Tokenization
tokenizers==0.22.2
sentencepiece==0.2.1

# Evaluation
# lm-eval is installed from source (lm-evaluation-harness/), not via pip
# lm_eval==0.4.12.dev0

# Attention kernels
flash-attn==2.8.3

# Numerics
numpy==2.2.6
scipy==1.17.1
